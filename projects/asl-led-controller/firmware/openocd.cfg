# OpenOCD config for STM32 Nucleo-G474RE
# Connects via the built-in ST-Link V3 (the USB port on the board)

source [find interface/stlink.cfg]
source [find target/stm32g4x.cfg]

# Optional: reduce speed if you get connection errors
# adapter speed 1000
